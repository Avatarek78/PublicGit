$(function(){    var sticky = $('.sticky').offset().top;    var stickyHeight = ($('.sticky').height());    var topOffset = 10;    $(window).scroll(function(){ // scroll event        var scroll_top = $(window).scrollTop() + topOffset; // returns number        // 500 je výška sticky        if ($(window).height() > stickyHeight && sticky < scroll_top){            $('.sticky').css({ position: 'fixed', top: topOffset });        }        else {            $('.sticky').css('position','static');        }    });});